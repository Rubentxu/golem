# NATS Server configuration with JetStream enabled
# For local development and TCK testing

server_name: golem-dev

jetstream {
  store_dir: /data/jetstream
  max_mem: 1G
}

# Allow telemetry endpoints (for health checks)
http_port: 8222
