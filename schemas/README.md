# schemas

Event schemas de GOLEM. Fuente de verdad:
`golem-documentation/06_API_SPECS/` (p. ej. `event-envelope.schema.json`).
La validación de envelopes contra schema entra en CI en M0/M1; nada se
duplica a mano en este directorio.
